const port = 4000;
const endpoint = `http://localhost:${port}`;
const prodEndpoint = '';

// API KEYS (Only keys that can be exposed to the public should  be included here)
const googleMapsKey = 'AIzaSyC7CHeyHtI2OxgYa6oO77JSO-rikWrs0Mw';

export { port, endpoint, prodEndpoint, googleMapsKey };
